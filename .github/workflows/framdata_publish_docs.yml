name: Deploy fram-data Documentation

on:
#  push:
#    branches:
#      - main
#    paths:
#      - 'docs/**'
#      - 'framcore/**'
#      - 'mkdocs.yml'
  workflow_dispatch:

jobs:
  deploy-docs:
    uses: NVE/fram-shared/.github/workflows/template_publish_docs.yml@main
    permissions:
      contents: write
    with:
      package_name: 'framdata'
      source_paths: '["docs/**", "framdata/**", "mkdocs.yml"]'
